"""SatsFlow security: password hashing and encrypted local storage.

Modules:
    passwords   — Argon2id hashing, verify, rehash detection
    crypto      — AES-256-GCM vault, Argon2id KDF, SecureVault class
"""
